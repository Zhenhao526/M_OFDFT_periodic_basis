# S2/G2 density-expansion acceptance policy R1

This analysis-only revision changes the decision scope, not the historical evidence. The dense-grid R1, joint-SVD R1 and null-envelope R1 rejection records remain immutable and continue to report their registered rank, conditioning and principal-angle failures.

For the present target—representing the density and evaluating the registered non-self-consistent operators—rank, condition, retained spectral margin and near-null-subspace angles are diagnostic. They do not veto a candidate whose electron number, density, component energies, fixed-WT total, eggbox energy, pseudoforces and cross-grid changes all pass their already registered thresholds.

The revision starts no solver and recomputes no density. It reads byte-bound committed evidence and reclassifies only the policy disposition. Acceptance means that the fixed 23-function `r08_eta100_complementary` candidate is accepted for the Al density-expansion scope. It does not claim unique or well-conditioned coefficients, does not erase the historical rejections, does not close G2c performance or Mg coverage, and does not start S3.

Git closure is base -> implementation -> config-only preregistration -> four-file output-only evidence.
